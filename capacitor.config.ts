import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.robertngo.angular',
  appName: 'angular',
  webDir: 'dist/angular/browser',
};

export default config;
